export function loadPiModule(packageRoot: string): Promise<Record<string, unknown>>;
export function readPiPackageVersion(packageRoot: string): string;
export function resolvePiPackageEntry(packageRoot: string): string;

export function renderPromptsTab(): string {
  return `        <section id="main-prompts" class="pane active">
          <div id="promptProfile">Loading...</div>
          <p class="muted" id="prompt-status"></p>
        </section>`;
}
